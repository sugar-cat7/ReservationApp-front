import RegisterSpace from '../../components/RegisterSpace';
import Layout from '../../components/Layout';
const RegisterSpaces: React.FC = () => {
  return (
    <Layout title="スペース追加">
      <RegisterSpace />
    </Layout>
  );
};

export default RegisterSpaces;
